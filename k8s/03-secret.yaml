# ── TEMPLATE — replace placeholder values before applying ──────────────────
# To create the secret safely without committing real values, use:
#
#   kubectl create secret generic agentic-ops-secrets \
#     --namespace dev \
#     --from-literal=OPENAI_API_KEY=sk-... \
#     --from-literal=LANGSMITH_API_KEY=lsv2_... \
#     --from-literal=SLACK_WEBHOOK_URL=https://hooks.slack.com/... \
#     --from-literal=SMTP_USER=you@gmail.com \
#     --from-literal=SMTP_PASSWORD=your-app-password \
#     --from-literal=NOTIFY_EMAIL_TO=recipient@example.com \
#     --from-literal=POSTGRES_PASSWORD=agentic
#
# The YAML below is a reference — values are base64(placeholder).
# DO NOT commit real secrets to git.
apiVersion: v1
kind: Secret
metadata:
  name: agentic-ops-secrets
  namespace: dev
  labels:
    app.kubernetes.io/name: agentic-ops
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_ME"
  LANGSMITH_API_KEY: "REPLACE_ME"
  SLACK_WEBHOOK_URL: ""
  SMTP_USER: ""
  SMTP_PASSWORD: ""
  NOTIFY_EMAIL_TO: ""
  POSTGRES_PASSWORD: "agentic"
